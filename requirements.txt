# requirements for deployment (Python dependencies)
# include same packages as streamlit-runner/requirements.txt

flask==2.3.3
subprocess32==3.5.4
# Node-based tests use npx/playwright; no Python playwright dependency needed
# pytest and pytest-playwright are only required if you run the Python test
# suite locally and can be installed separately if desired.
